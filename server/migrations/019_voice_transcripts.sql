-- Расшифровка голосовых сообщений в текст.
-- Текст хранится рядом с сообщением: расшифровка делается один раз по запросу
-- участника диалога, затем отдаётся из базы (повторных обращений к модели нет).
ALTER TABLE messages ADD COLUMN IF NOT EXISTS transcript text;
